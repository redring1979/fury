namespace Numerics
{
    public static class MathHelper
    {
        internal static double Epsilon = 0.0001;

        public static double Sqrt(double d) => Math.Sqrt(d);

        public const double PI = Math.PI;
    }
}